# Colab-aligned extras only.
#
# Do not list torch, torchvision, numpy, opencv, pandas, scikit-learn, pillow,
# or matplotlib here. Colab already provides those packages, and resolving them
# through pip can downgrade or replace the runtime stack.
#
# For the notebook benchmark, run:
#   python scripts/install_colab_deps.py
#
# That script installs facenet-pytorch with --no-deps so its old torch/numpy
# constraints do not override Colab.
gdown>=5.2.0
seaborn>=0.13.0
tqdm>=4.66.0
